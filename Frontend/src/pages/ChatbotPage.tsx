// The Chainlit iframe is rendered persistently in Sidebar.tsx
// so it survives tab navigation. This component is kept as a
// route placeholder so React Router resolves /app/chat.
export function ChatbotPage() {
  return null;
}
