# Setup Guide

## Quick Setup

1. **Install dependencies:**

   ```bash
   cd LumenIQ/Frontend
   npm install
   ```

2. **Start the dev server:**

   ```bash
   npm run dev
   ```

3. **Open in browser** at `http://localhost:5173` (check terminal if port is different)
